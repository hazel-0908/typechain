//console.log("hello");

const name = "Poburi",
    age = 24,
    gender = "female";

const sayHi = (name, age, gender) => {
    console.log(`Hello ${name}, you ar ${age}, you are a ${gender}`);
};

sayHi(name, age, gender);

export {};